<h1 align="center">Класс <i>UIExStackPanel</i></h1>

`UIExStackPanel` - это контейнер компоновки, который располагает дочерние элементы в одну линию (стопку) по вертикали или по горизонтали.
`UIExStackPanel` определяет поле `public Styles.StyleLayoutContainerStackPanel StyleLayoutStackPanel { get; set; }`, которое хранит все стили компоновки для данного класса.

```csharp
public class StyleLayoutContainerStackPanel : IStyleLayoutContainer, IStyleCopy<StyleLayoutContainerStackPanel>
{
    public Enums.Orientation Orientation = Enums.Orientation.Vertical;
    public Enums.Alignment JustifyContent = Enums.Alignment.Start;
    public Enums.Alignment AlignItems = Enums.Alignment.Start;
    public StyleDimension Spacing = StyleDimension.Empty;
    public bool Reverse = false;

    public StyleLayoutContainerStackPanel Copy() { /* ... */ }
}
```

- `Orientation`:
  - Отвечает за то, по какой оси будет располагаться дочерние элементы.
  - Может принимать два значения: `Enums.Orientation.Vertical` и `Enums.Orientation.Horizontal`.
- `JustifyContent`:
  - Отвечает за то, как будут выравниваться вложенные элементы относительно основной оси:
    - `Enums.Alignment.Start` - элементы будут располагаться с стартовой точки основной оси `UIExStackPanel`. Размер элементов не меняется.
    - `Enums.Alignment.Center` - элементы будут располагаться по центру основной оси `UIExStackPanel`. Размер элементов не меняется.
    - `Enums.Alignment.End` - элементы будут располагаться с конечной точки основной оси `UIExStackPanel`. Размер элементов не меняется.
    - `Enums.Alignment.Stretch` - элементы будут располагаться с стартовой точки основной оси `UIExStackPanel`. `UIExStackPanel` поровну разделит свое пространство по основной на количество дочерних элементов и растянет Height или Width (зависит от оси) элементов, чтобы они заняли все доступное пространство основной оси.
- `AlignItems`:
  - Отвечает за то, как буду выравниваться вложенные элемент относительно поперечной оси.
  - Полный аналог `JustifyContent` для поперечной оси.
- `Spacing`:
  - Отвечает за пространство между дочерними элементами `UIExStackPanel`.
  - Работает даже если `JustifyContent` и/или `AlignItems` == `Enums.Alignment.Stretch`
- `Reverse`:
  - Флаг, отвечающий за расположение элементов в реверсивном порядке.
  - При `true` — все элементы будут располагаться в обратном порядке, сохраняя все прочие характеристики стиля.

----

<h2 align="center">Пример использования <i>UIExStackPanel</i></h2>

<details>
<summary>Пример создания пользовательского интерфейса</summary>

```csharp
using Terraria.GameContent.UI.Elements;
using Terraria.UI;

namespace UIeXtenstion;

public class ExampleMenuUI : UIExModSystem<ExamplePanelUI> { }

public class ExamplePanelUI : UIState
{
    public override void OnInitialize()
    {
        UIPanel panel = new UIPanel();
        SetPrecentSizeCenter(0.8f, 0.8f, panel);

        UIExStackPanel HStackPanel = new();
        SetPrecentSizeCenter(1f, 1f, HStackPanel);
        HStackPanel.StyleLayoutStackPanel.Set(
            orientation:        Enums.Orientation.Horizontal,
            justifyContent:     Enums.Alignment.Stretch, 
            alignItems:         Enums.Alignment.Stretch,
            spacing:            new StyleDimension(0f, 0.05f),
            reverse:            false);
        
        panel.Append(HStackPanel);

        //////

        UIPanel HPanel1 = new(), HPanel2 = new(), HPanel3 = new(), HPanel4 = new();
        HStackPanel.Append(HPanel1);
        HStackPanel.Append(HPanel2);
        HStackPanel.Append(HPanel3);
        HStackPanel.Append(HPanel4);

        /////

        UIExStackPanel VStackPanel1 = new(), VStackPanel2 = new(), VStackPanel3 = new(), VStackPanel4 = new();
        SetPrecentSizeCenter(1f, 1f, VStackPanel1, VStackPanel2, VStackPanel3, VStackPanel4);
        HPanel1.Append(VStackPanel1);
        HPanel2.Append(VStackPanel2);
        HPanel3.Append(VStackPanel3);
        HPanel4.Append(VStackPanel4);

        VStackPanel1.StyleLayoutStackPanel.Set(
            orientation:        Enums.Orientation.Vertical,
            justifyContent:     Enums.Alignment.Start,
            alignItems:         Enums.Alignment.Start,
            spacing:            new StyleDimension(0f, 0.1f),
            reverse:            false);

        AddCubs(VStackPanel1, pixelSize: 64f, count: 3);

        ////

        VStackPanel2.StyleLayoutStackPanel.Set(
            orientation:        Enums.Orientation.Vertical,
            justifyContent:     Enums.Alignment.Center,
            alignItems:         Enums.Alignment.Center,
            spacing:            new StyleDimension(0f, 0.15f),
            reverse:            true);

        AddCubs(VStackPanel2, pixelSize: 16f);
        AddCubs(VStackPanel2, pixelSize: 32f);
        AddCubs(VStackPanel2, pixelSize: 48f);
        AddCubs(VStackPanel2, pixelSize: 64f);

        ////

        VStackPanel3.StyleLayoutStackPanel.Set(
            orientation:        Enums.Orientation.Vertical,
            justifyContent:     Enums.Alignment.End,
            alignItems:         Enums.Alignment.End,
            spacing:            new StyleDimension(0f, 0.20f),
            reverse:            false);

        AddCubs(VStackPanel3, pixelSize: 32f);
        AddCubs(VStackPanel3, pixelSize: 48f);
        AddCubs(VStackPanel3, pixelSize: 64f);

        ////

        VStackPanel4.StyleLayoutStackPanel.Set(
            orientation:        Enums.Orientation.Vertical,
            justifyContent:     Enums.Alignment.Stretch,
            alignItems:         Enums.Alignment.Stretch,
            spacing:            new StyleDimension(0f, 0.20f),
            reverse:            false);

        AddCubs(VStackPanel4, pixelSize: 60f);
        AddCubs(VStackPanel4, pixelSize: 50f);
        AddCubs(VStackPanel4, pixelSize: 70f);

        ////

        Append(panel);
    }

    public void SetPrecentSizeCenter(float width, float height, params UIElement[] elements)
    {
        foreach (var element in elements)
        {
            element.Width.Set(0f, width);
            element.Height.Set(0f, height);
            element.HAlign = 0.5f;
            element.VAlign = 0.5f;
        }
    }

    public void AddCubs(UIElement element, float pixelSize, int count = 1)
    {
        for (int i = 0; i < count; i++)
        {
            UIPanel panel = new UIPanel();
            panel.Width.Set(pixelSize, 0f);
            panel.Height.Set(pixelSize, 0f);
            element.Append(panel);
        }
    }
}
```

</details>

<p align="center">
  <img src="../../../Images/LibTutorial/UIExStackPanel/img1.png" width="960" height="512">
  <br>
  <em>Рис. 1.1. Визуальное отображения работы кода</em>
</p>

----

Первое, что вы видите в коде:
```csharp
UIPanel panel = new UIPanel();
SetPrecentSizeCenter(0.8f, 0.8f, panel);
```
Это обычная панель, которую будет добавлена в `UIState`. На ней будут располагаться `UIExStackPanel`.
На изображение это самая большая и самая прозрачная панель.<br><br>
`SetPrecentSizeCenter` - это вспомогательный метод, которого нет в самой библиотеке, и который определен ниже в этом же классе.
Он получает N `UIElement`, устанавливаем им переданную высоту и ширину и центрирует. В данном случае наша `panel` имеет размер: 80%х80% экрана.<br><br>

----

Далее в коде идет первый `UIExStackPanel`:
```csharp
UIExStackPanel HStackPanel = new();
SetPrecentSizeCenter(1f, 1f, HStackPanel);
HStackPanel.StyleLayoutStackPanel.Set(
    orientation:        Enums.Orientation.Horizontal,
    justifyContent:     Enums.Alignment.Stretch, 
    alignItems:         Enums.Alignment.Stretch,
    spacing:            new StyleDimension(0f, 0.05f),
    reverse:            false);

panel.Append(HStackPanel);

//////

UIPanel HPanel1 = new(), HPanel2 = new(), HPanel3 = new(), HPanel4 = new();
HStackPanel.Append(HPanel1);
HStackPanel.Append(HPanel2);
HStackPanel.Append(HPanel3);
HStackPanel.Append(HPanel4);
```

- `SetPrecentSizeCenter(1f, 1f, HStackPanel);` - устанавливает размеры `HStackPanel`: 100% от родительского элемента.
- Ниже мы добавляем ее в основную панель `panel.Append(HStackPanel);`, что означает, что `HStackPanel` будет компоновать элементы по всему созданному интерфейсу.
- `Set` -это метод, который есть у любого класса стиля в библиотеке. Каждый Set стиля требует передать в него значения для **ВСЕХ** существующих полей класса стиля.
- Его стили:
  - `Orientation`: горизонтальная.
  - `JustifyContent` (выравнивание элементов по основной оси) (основная ось - горизонтальная): `stretch` - элементы занимают все доступное пространство по ширине.
  - `AlignItems` (выравнивание элементов по поперечной оси) (поперечной ось - вертикальная): `stretch` - элементы занимают все доступное пространство по высоте.
  - `Spacing`: 5% от размера основной оси контейнера (в данном случае: 5% от ширины `HStackPanel` -> `panel`).
  - `Reverse`: false - элементы располагаются в том порядке, в каком они будут добавлены в `HStackPanel`.


Ниже в `HStackPanel` добавлены 4 `HPanel_N`:
- `HStackPanel` сперва выделит место под `Spacing (5% * HStackPanel.Width * (HStackPanel.GetWithLayoutElementsCount() - 1))`
- Затем посчитает свободное пространство и поделит его между этими элементами.
- Результат этого вы можете посмотреть самостоятельно. На изображении это 4 самые высокие панели.

----

```csharp
UIExStackPanel VStackPanel1 = new(), VStackPanel2 = new(), VStackPanel3 = new(), VStackPanel4 = new();
SetPrecentSizeCenter(1f, 1f, VStackPanel1, VStackPanel2, VStackPanel3, VStackPanel4);
HPanel1.Append(VStackPanel1);
HPanel2.Append(VStackPanel2);
HPanel3.Append(VStackPanel3);
HPanel4.Append(VStackPanel4);
```

К каждой из созданных до этого `HPanel_N` создается свой `UIExStackPanel (VStackPanel_N)`. 
Далее каждый из `VStackPanel_N` получает размеры 100% своего родителя и добавляются в соответствующие панель `HPanel_N`<br>

----

Хочу на этом месте подвести промежуточный итог.
На данный момент у нас система такая:
- `UIPanel panel` - имеет 80% размера экрана (самая большая панель на изображении). Внутри нее расположен:<br>
  - `UIExStackPanel HStackPanel` - имеет 100% размера `panel` (не имеет визуальной части). Внутри него расположены:<br>
    - `UIPanel HPanel1 - имеет 100% высоты `HStackPanel` и 25% от свободного пространства (после `Spacing`) (первая слева панель с самой большой высотой). Внутри нее:<br>
      - `UIExStackPanel VStackPanel1` - имеет 100% размера `HPanel1` (не имеет визуальной части).<br>
    - `UIPanel HPanel2` - имеет 100% высоты `HStackPanel` и 25% от свободного пространства (после `Spacing`) (вторая слева панель с самой большой высотой). Внутри нее:<br>
      - `UIExStackPanel VStackPanel2` - имеет 100% размера `HPanel2` (не имеет визуальной части).<br>
    - `UIPanel HPanel3` - имеет 100% высоты `HStackPanel` и 25% от свободного пространства (после `Spacing`) (третья слева панель с самой большой высотой). Внутри нее:<br>
      - `UIExStackPanel VStackPanel3` - имеет 100% размера `HPanel3` (не имеет визуальной части).<br>
    - `UIPanel HPanel4` - имеет 100% высоты `HStackPanel` и 25% от свободного пространства (после `Spacing`) (четвертая слева панель с самой большой высотой). Внутри нее:<br>
      - `UIExStackPanel VStackPanel4` - имеет 100% размера `HPanel4` (не имеет визуальной части).<br>

<br>

<table align="center"> 
    <tr> <th>Элемент</th> <th>Размер / положение</th> <th>Назначение</th> </tr> 
    <tr> 
        <td><code>UIPanel panel</code></td> 
        <td>80% экрана</td> 
        <td>Корневая панель</td> </tr> 
    <tr> 
        <td><code>UIExStackPanel HStackPanel</code></td> 
        <td>100% × 100% <code>panel</code></td> 
        <td>Горизонтальное расположение панелей</td> </tr> 
    <tr> 
        <td><code>UIPanel HPanel_1–4</code>
        </td> <td>100% высоты<br>25% свободной ширины</td> 
        <td>Четыре горизонтальные секции</td> </tr> 
    <tr> 
        <td><code>UIExStackPanel VStackPanel_1–4</code>
        </td> <td>100% × 100% <code>HPanel_1–4</code></td> 
        <td>Вертикальное расположение элементов внутри секции</td> </tr> 
</table>

----

Дальше для каждой `VStackPanel_1–4` задается стиль компоновки и добавляются другие `UIPanel` с помощью метода:
```csharp
public void AddCubs(UIElement element, float pixelSize, int count = 1)
{
    for (int i = 0; i < count; i++)
    {
        UIPanel panel = new UIPanel();
        panel.Width.Set(pixelSize, 0f);
        panel.Height.Set(pixelSize, 0f);
        element.Append(panel);
    }
}
```
Данный метод также создан специально для данного примера не существует в самой библиотеке.<br><br>

Нет смысла разбирать все. Разберем только одну часть кода, где используется в методе `Set` значение `reverse == true`<br>

```csharp
VStackPanel2.StyleLayoutStackPanel.Set(
    orientation:        Enums.Orientation.Vertical,
    justifyContent:     Enums.Alignment.Center,
    alignItems:         Enums.Alignment.Center,
    spacing:            new StyleDimension(0f, 0.15f),
    reverse:            true);

AddCubs(VStackPanel2, pixelSize: 16f);
AddCubs(VStackPanel2, pixelSize: 32f);
AddCubs(VStackPanel2, pixelSize: 48f);
AddCubs(VStackPanel2, pixelSize: 64f);
```

Результат данной части кода вы можете видеть на изображении внутри 2-й слева длинной панели.

- Стили VStackPanel2:
  - `Orientation`: вертикальная.
  - `JustifyContent` (выравнивание элементов по основной оси) (основная ось - вертикальная): `сenter` - элемент располагаются по центру вертикально.
  - `AlignItems` (выравнивание элементов по поперечной оси) (поперечной ось - горизонтальная): `сenter` - элемент располагаются по центру горионтально.
  - `Spacing`: 15% от размера основной оси контейнера (в данном случае: 5% от высоты `VStackPanel2` -> `HPanel2`).
  - `Reverse`: `true` - элементы располагаются в обратном порядке, по сравнению с порядком добавления в `VStackPanel2`. Можно заметить, что элементы добавляются в `VStackPanel2` так: 16f, 32f, 48f, 64f. Если бы `Reverse` был бы равен `false`, то элементы в таком порядке и располагались бы снизу вверх. Но на изображении вы можете видеть, что сверху - самый большой элемент, снизу - самый маленький.

----

<table align="center" width="100%">
  <tr>
    <td align="left"><b>Предыдущая часть:</b></td>
    <td align="left">
      <a 
        href="2.2-UIExCanvas.md">
        Раздел II: Элементы компоновки. Часть 2: «UIExCanvas»
      </a>
    </td>
  </tr>
  <tr>
    <td align="left"><b>Следующая часть:</b></td>
    <td align="left">
      <a 
        href="2.4-UIExDockPanel.md">
        Раздел II: Элементы компоновки. Часть 4: «UIExDockPanel»
      </a>
    </td>
  </tr>
  <tr>
    <td align="left"><b>Содержание:</b></td>
    <td align="left"><a href="../README.md">Перейти к оглавлению</a></td>
  </tr>
</table>
