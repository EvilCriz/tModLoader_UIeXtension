<h1 align="center">Library Creation</h1>

---

* This is the library version from its first release.<br>
* The library is at the Alpha development stage.<br>
* All tools are still being tested.

---

<h2 align="center">Added Classes / Enumerations</h2>

The following were added to the library upon release:

1. UI element classes (non-layout containers):

* `UIExElement`.<br><br>

<!-- -->

2. Layout container classes:

* `UIExLayout`;
* `UIExCanvas`;
* `UIExFlowPanel`
* `UIExStackPanel`;
* `UIExWrapPanel`;
* `UIExDockPanel`;
* `UIExGridBase`;
* `UIExGrid`;
* `UIExUniformGrid`.<br><br>

<!-- -->

3. Style classes:

* `StyleBase`;
* `StyleVisualBase`;
* `StyleLayoutContainerBase`;
* `StyleLayoutChildBase`;
* `StyleTmlBase`;
* `StyleVisualElement`;
* `StyleLayoutContainer`;
* `StyleLayoutContainerCanvas`;
* `StyleLayoutContainerStackPanel`;
* `StyleLayoutContainerWrapPanel`;
* `StyleLayoutContainerDockPanel`;
* `StyleLayoutContainerGrid`;
* `StyleLayoutContainerUniformGrid`;
* `StyleLayoutChild`;
* `StyleLayoutChildCanvas`;
* `StyleLayoutChildDockPanel`;
* `StyleLayoutChildGrid`;
* `StyleLayoutChildUniformGrid`;
* `StyleTmlUIPanel`.<br><br>

<!-- -->

4. Helper classes for layout:

* `RectangleLayoutContext`;
* `UIExRectangle`;
* `LayoutDebugRectangle`.<br><br>

<!-- -->

5. Helper classes for styles:

* `StyleSide`;
* `UIExGridLength`;
* `UIExThickness`.<br><br>

<!-- -->

6. Enumerations:

* `UIExOrientation`;
* `UIExAlignment`;
* `UIExSide`;
* `UIExGridLengthType`.<br><br>

<!-- -->

7. Helper classes: analogues of **tModLoader**:

* `UIExModSystem`.<br><br>

<!-- -->

8. Static classes for extension methods:

* `UIElementExtensions`.<br><br>

<!-- -->

9. Static utility classes:

* `UtilsStyles`;
* `UtilsLayout`;
* `UtilsFinder`.<br><br>

<!-- -->

---

<h2 align="center">Added Articles to the Library's Tutorials</h2>

Most of the classes and enumerations listed above are described in the tutorials.<br>

Added articles:

1. Section 1:

* «Library Structure»;
* «The `UIExModSystem` Class»;
* «Style Structure»;
* «Additional Resources for Learning».<br><br>

<!-- -->

2. Section 2:

* «The `UIExLayout` Class. Layout Basics»;
* «The `UIExCanvas` Class»;
* «The `UIExStackPanel` Class»;
* «The `UIExWrapPanel` Class»;
* «The `UIExDockPanel` Class»;
* «The `UIExGrid` Class»;
* «The `UIExUniformGrid` Class»;
* «Optimizing Layout Elements»;
* «The Lifecycle of Layout Elements»;
* «Recommendations for Creating Your Own Layout Containers».<br><br>

<!-- -->

3. Section 3:

* «The `UIExElement` Class» (not fully written yet. The `UIExElement` class is currently only partially implemented).<br><br>

---

<h2 align="center">Additional Resources</h2>

* The source code with all comments (in English) has been added.
* An example mod has been added (a very raw version, containing only the UI implementation from the tutorials)

---

Please support this project: <a href="https://ko-fi.com/evilcriz">Ko-fi</a>, <a href="https://www.patreon.com/cw/EvilCriz">Patreon</a><br>

* Thanks to your support, I can continue working on this and other projects.
* Thank you!<br>

**Important!!** If you are associated with the Russian Federation, supporting this project may be **dangerous for you**!<br>
I am located in and live in Ukraine.

---

<table align="center" width="100%">
  <tr>
    <td align="left"><b>Contents:</b></td>
    <td align="left"><a href="../README.md">Go to the table of contents</a></td>
  </tr>
</table>
