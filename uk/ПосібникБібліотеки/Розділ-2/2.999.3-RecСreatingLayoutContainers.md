<h1 align="center">Рекомендації щодо створення власного контейнерного макету</h1>

----

- Бібліотека припускає, що ви вже навчилися [базовий посібник tModLoader Користувач-інтерфейс](https://github.com/tModLoader/tModLoader/wiki/Basic-UI-Element)<br>
- Бібліотека припускає, що ви вже навчилися [Розширений посібник з налаштування інтерфейсу користувача](https://github.com/tModLoader/tModLoader/wiki/Advanced-guide-to-custom-UI)<br>
- У статті запропоновано, що ви вивчили статтю: ["Схема стилю"](../Розділ-1/1.3-StyleStructure.md)
- У статті запропоновано, що ви вивчили статтю: [клас UIExLayoutОснови макета](2.1-UIExLayout.md)
- У статті запропоновано, що ви вивчили статтю: [«Оптимізація елементів макета»](2.999.1-Optimization.md)
- У статті запропоновано, що ви вивчили статтю: [«Життєвий цикл елементів макета»](2.999.2-LayoutLifeСycle.md)

----
<br>

----

<h2 align="center">Створення стилів макета</h2>

Перед створенням контейнерного класу слід відповісти на два питання:
1. Чи визначить його стиль контейнера?
2. Чи буде контейнер визначити його стиль для в'язаних предметів?<br>
<!-- -->
1. Всі стилі контейнерних макетів повинні бути успадковані від класу. `UIeXtension.Styles.Base.StyleLayoutContainerBase`.
2. Всі стилі макета індивідуального елемента повинні бути успадковані з класу. `UIeXtension.Styles.Base.StyleLayoutChildBase`.<br>

Самі заняття. `StyleLayoutContainerBase` і `StyleLayoutChildBase` клас `UIeXtension.Styles.Base.StyleBase`.<br>

```csharp
public abstract class StyleBase
{
    protected abstract StyleBase Fabricate();

    protected abstract void CopyBase(StyleBase style);

    protected virtual void CopyInnerStylesBase(StyleBase style) { }

    protected internal T GetCopyBase<T>() where T : StyleBase, new()
    {
        T copy = new();
        copy.CopyBase(this);
        copy.CopyInnerStylesBase(this);
        return copy;
    }


    protected internal StyleBase GetCopyBase()
    { 
        StyleBase copy = Fabricate();
        copy.CopyBase(this);
        copy.CopyInnerStylesBase(this);
        return copy;
    }


    public bool EqualsStylesFields(StyleBase other)
    {
        if (!EqualsFields(other))
            return false;

        return EqualsInnerStylesFields(other);
    }

    protected abstract bool EqualsFields(StyleBase other);


    protected bool EqualsInnerStylesFields<TStyleBase>(Dictionary<Type, TStyleBase> styles1, Dictionary<Type, TStyleBase> styles2) 
        where TStyleBase : StyleBase
    {
        if (styles1.Count != styles2.Count)
            return false;

        foreach (var key in styles1.Keys)
        {
            if (!styles2.ContainsKey(key))
                return false;

            if (!styles1[key].EqualsFields(styles2[key]))
                return false;
        }

        return true;
    }


    protected virtual bool EqualsInnerStylesFields(StyleBase other) => true;
}
```
<br>

Всі приклади з класу. `StyleLayoutContainerUniformGrid`.

1. Методика `Fabricate` Ви повинні повернути копію стильного класу, який ви створили.<br>
```csharp
protected override Base.StyleBase Fabricate() => new StyleLayoutContainerUniformGrid();
```
<br>

2. Методика `CopyBase` Рекомендовано в реалізації:<br>
```csharp
protected override void CopyBase(Base.StyleBase style)
{
    if (style is StyleLayoutContainerUniformGrid style2)
        Copy(style2);
}
```
<br>

3. У прикладі використовується метод `Copy`Незважаючи на те, що бібліотека не вимагає створення цього методу, його створення є дуже бажаним:
```csharp
public void Copy(StyleLayoutContainerUniformGrid style)
{
    RowsCount = style.RowsCount;
    ColumnsCount = style.ColumnsCount;

    RowsAlignment = style.RowsAlignment;
    ColumnsAlignment = style.ColumnsAlignment;

    RowsSpace = style.RowsSpace;
    ColumnsSpace = style.ColumnsSpace;
}
```
<br>

4. З методами `GetCopyBase`, `EqualsStylesFields`, `EqualsInnerStylesFields` Ви не повинні нічого робити. Вони вже повністю реалізовані.<br><br>

5. Методика `EqualsFields(StyleBase other)` Порівняння всіх полів поточного і переданого стилю:
```csharp
protected override bool EqualsFields(Base.StyleBase other)
{
    if (other is StyleLayoutContainerUniformGrid style)
    {
        if (RowsCount != style.RowsCount ||
            ColumnsCount != style.ColumnsCount)
            return false;

        if (RowsAlignment != style.RowsAlignment ||
            ColumnsAlignment != style.ColumnsAlignment)
            return false;

        if (!Utils.UtilsStyles.EqualsStyleDimensionFields(RowsSpace, style.RowsSpace) ||
            !Utils.UtilsStyles.EqualsStyleDimensionFields(ColumnsSpace, style.ColumnsSpace))
            return false;

        return true;
    }

    return false;
}
```

----

Не визначені наступні методи в класі `StyleBase`Кожен з класів бібліотеки містить:
1. Методика `Copy` вже описано вище.<br><br>
2. Методи. `Set`, `SetAllProperties`Обидва методи приймають всі поля класу. `Set` Це дає кожному з них параметра за замовчуванням:
```csharp
public void Set(
    int rowsCount = 0,
    int columnsCount = 0,
    Enums.UIExAlignment rowsAlignment = Enums.UIExAlignment.Start,
    Enums.UIExAlignment columnsAlignment = Enums.UIExAlignment.Start,
    StyleDimension rowsSpace = default(StyleDimension),
    StyleDimension columnSpace = default(StyleDimension))
        => SetAllFields(
            rowsCount,
            columnsCount,
            rowsAlignment,
            columnsAlignment,
            rowsSpace,
            columnSpace);

protected void SetAllFields(
    int rowsCount,
    int columnsCount,
    Enums.UIExAlignment rowsAlignment,
    Enums.UIExAlignment columnsAlignment,
    StyleDimension rowsSpace,
    StyleDimension columnSpace)
{
    RowsCount = rowsCount;
    ColumnsCount = columnsCount;

    RowsAlignment = rowsAlignment;
    ColumnsAlignment = columnsAlignment;

    RowsSpace = rowsSpace;
    ColumnsSpace = columnSpace;
}
```
<br>

3. Методика `THIS_CLASS GetCopy()` повертає об'єкт нового стилю з усіма поточними полями. `CopyBase` (див. вище), така реалізація досить:
```csharp
public StyleLayoutContainerUniformGrid GetCopy()
    => GetCopyBase<StyleLayoutContainerUniformGrid>();
```

----

Також у всіх класах стилю цієї бібліотеки є два дизайнери:
1. За замовчуванням (обов'язкова вимога).
2. Прийняти всі поля класу (з значенням за замовчуванням).
```csharp
public StyleLayoutContainerUniformGrid() { }

public StyleLayoutContainerUniformGrid(
    int rowsCount = 0,
    int columnsCount = 0,
    Enums.UIExAlignment rowsAlignment = Enums.UIExAlignment.Start,
    Enums.UIExAlignment columnsAlignment = Enums.UIExAlignment.Start,
    StyleDimension rowsSpace = default(StyleDimension),
    StyleDimension columnSpace = default(StyleDimension))
        => SetAllFields(
            rowsCount,
            columnsCount,
            rowsAlignment,
            columnsAlignment,
            rowsSpace,
            columnSpace);
```

----

<h2 align="center">Реєстрація стилів макета</h2>

1. Контейнерні стилі повинні бути зареєстровані в класі `StyleLayoutContainer`.
2. Стилі макета індивідуального елемента необхідно зареєструватися в класі. `StyleLayoutChild`.<br>

У бібліотеці всі класи контейнерів зареєстровані в `StyleLayoutContainer` (символ до) `StyleLayoutChild`):
```csharp
public partial class StyleLayoutContainer : Base.StyleLayoutContainerBase
{
    private readonly System.Collections.Generic.Dictionary<System.Type, Base.StyleLayoutContainerBase> _styles = new();

    public StyleLayoutContainerCanvas Canvas() => GetOrCreateStyle<StyleLayoutContainerCanvas>();
    public StyleLayoutContainerStackPanel StackPanel() => GetOrCreateStyle<StyleLayoutContainerStackPanel>();
    public StyleLayoutContainerDockPanel DockPanel() => GetOrCreateStyle<StyleLayoutContainerDockPanel>();
    public StyleLayoutContainerWrapPanel WrapPanel() => GetOrCreateStyle<StyleLayoutContainerWrapPanel>();
    public StyleLayoutContainerGrid Grid() => GetOrCreateStyle<StyleLayoutContainerGrid>();
    public StyleLayoutContainerUniformGrid UniformGrid() => GetOrCreateStyle<StyleLayoutContainerUniformGrid>();

    public T GetOrCreateStyle<T>() where T : Base.StyleLayoutContainerBase, new()
    {
        if (!_styles.TryGetValue(typeof(T), out Base.StyleLayoutContainerBase value))
        {
            value = new T();
            _styles.Add(typeof(T), value);
        }

        return (T)value;
    }
}
```

Щоб зареєструвати свій стиль, потрібно використовувати метод розширення для цього класу, наприклад:
```csharp
using UIeXtension;
using UIeXtension.Styles;
using UIeXtension.Styles.Base;

namespace Example;

// Ваш клас стилю контейнерного планування
public class StyleLayoutContainerExample : StyleLayoutContainerBase 
{ 
    /* 
    
    ... 

    */ 
}


// Ваш клас за допомогою методу розширення StyleLayoutContainer
public static class StyleLayoutContainerExtensions
{
    public static StyleLayoutContainerExample Example(this StyleLayoutContainer style)
        => style.GetOrCreateStyle<StyleLayoutContainerExample>();
}


// клас як приклад отримання вашого стилю
public class ExampleClass
{
    public void Initialize()
    {
        UIExGrid customLayout = new(); // апошні UIExGrid - макет контейнера.
        StyleLayoutContainerExample style = customLayout.StyleLayout.Example(); 
    }
}
```

----

<h2 align="center">У віртуальних методах</h2>

Більшість способів всіх класів контейнерів є віртуальними.<br>
У цій статті неможливо описати всі методи, які реалізують дану бібліотеку.<br>
Всі класи Допоміжні методи `UIExLayout` файлообмінник `UIExLayoutAuxiliaryMethods.cs`.<br>
Якщо ви плануєте створити власні посилання - рекомендується ознайомитися з даними файлів.<br><br>

Ви можете взяти всі інші етапи створення посилання з попередньої статті та вивчити код посилань на цю бібліотеку.

----

<table align="center" width="100%">
    <tr>
        <td align="left"><b>Попередня частина:</b></td>
        <td align="left">
          <a 
            href="2.999.2-LayoutLifeCycle.md">
            Розсилка IIЧастина 10: життєвий цикл композиційних елементів
          </a>
        </td>
    </tr>
    <tr>
        <td align="left"><b>Наступна частина:</b></td>
        <td align="left">
          <a 
            href="/секція 3/3.1-UIExElement.md">
            Розсилка III: Елементи інтерфейсу користувача Part 1: "Class" UIExElement»
          </a>
        </td>
    </tr>
    <tr>
        <td align="left"><b>Зміст:</b></td>
        <td align="left"><a href="../README.md">Перейти до таблиці змісту</a></td>
    </tr>
</table>
