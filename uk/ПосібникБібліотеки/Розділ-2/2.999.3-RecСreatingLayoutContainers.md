<h1 align="center">Рекомендації щодо створення власних контейнерів компонування</h1>

---

* Дана бібліотека передбачає, що ви вже вивчили [базовий посібник tModLoader щодо користувацького інтерфейсу](https://github.com/tModLoader/tModLoader/wiki/Basic-UI-Element)<br>
* Дана бібліотека передбачає, що ви вже вивчили [розширений посібник із налаштування користувацького інтерфейсу](https://github.com/tModLoader/tModLoader/wiki/Advanced-guide-to-custom-UI)<br>
* Дана стаття передбачає, що ви вивчили статтю: [«структура стилів»](../Розділ-1/1.3-StyleStructure.md)
* Дана стаття передбачає, що ви вивчили статтю: [«клас UIExLayout. Основи компонування»](2.1-UIExLayout.md)
* Дана стаття передбачає, що ви вивчили статтю: [«оптимізація елементів компонування»](2.999.1-Optimization.md)
* Дана стаття передбачає, що ви вивчили статтю: [«життєвий цикл елементів компонування»](2.999.2-LayoutLifeСycle.md)

---

<br>

---

<h2 align="center">Створення стилів компонування</h2>

Перед створенням власного класу контейнера ви повинні відповісти собі на два питання:

1. Чи буде контейнер визначати свій стиль компонування контейнера?
2. Чи буде контейнер визначати свій стиль для вкладених елементів?<br>

<!-- -->

1. Усі стилі компонування контейнера повинні успадковуватися від класу `UIeXtension.Styles.Base.StyleLayoutContainerBase`.
2. Усі стилі компонування окремого елемента повинні успадковуватися від класу `UIeXtension.Styles.Base.StyleLayoutChildBase`.<br>

Самі класи `StyleLayoutContainerBase` і `StyleLayoutChildBase` успадковують клас `UIeXtension.Styles.Base.StyleBase`.<br>

```csharp
public abstract class StyleBase
{
    protected abstract StyleBase Fabricate();

    protected abstract void CopyBase(StyleBase style);

    protected virtual void CopyInnerStylesBase(StyleBase style) { }

    protected internal T GetCopyBase<T>() where T : StyleBase, new()
    {
        T copy = new();
        copy.CopyBase(this);
        copy.CopyInnerStylesBase(this);
        return copy;
    }


    protected internal StyleBase GetCopyBase()
    { 
        StyleBase copy = Fabricate();
        copy.CopyBase(this);
        copy.CopyInnerStylesBase(this);
        return copy;
    }


    public bool EqualsStylesFields(StyleBase other)
    {
        if (!EqualsFields(other))
            return false;

        return EqualsInnerStylesFields(other);
    }

    protected abstract bool EqualsFields(StyleBase other);


    protected bool EqualsInnerStylesFields<TStyleBase>(Dictionary<Type, TStyleBase> styles1, Dictionary<Type, TStyleBase> styles2) 
        where TStyleBase : StyleBase
    {
        if (styles1.Count != styles2.Count)
            return false;

        foreach (var key in styles1.Keys)
        {
            if (!styles2.ContainsKey(key))
                return false;

            if (!styles1[key].EqualsFields(styles2[key]))
                return false;
        }

        return true;
    }


    protected virtual bool EqualsInnerStylesFields(StyleBase other) => true;
}
```

<br>

Усі приклади наведені з класу `StyleLayoutContainerUniformGrid`.

1. Метод `Fabricate` повинен повертати екземпляр створеного вами класу стилю.<br>

```csharp
protected override Base.StyleBase Fabricate() => new StyleLayoutContainerUniformGrid();
```

<br>

2. Метод `CopyBase` рекомендується реалізовувати наступним чином:<br>

```csharp
protected override void CopyBase(Base.StyleBase style)
{
    if (style is StyleLayoutContainerUniformGrid style2)
        Copy(style2);
}
```

<br>

3. У прикладі використано метод `Copy`. Хоча бібліотека не вимагає створення цього методу, його створення вкрай бажане:

```csharp
public void Copy(StyleLayoutContainerUniformGrid style)
{
    RowsCount = style.RowsCount;
    ColumnsCount = style.ColumnsCount;

    RowsAlignment = style.RowsAlignment;
    ColumnsAlignment = style.ColumnsAlignment;

    RowsSpace = style.RowsSpace;
    ColumnsSpace = style.ColumnsSpace;
}
```

<br>

4. З методами `GetCopyBase`, `EqualsStylesFields`, `EqualsInnerStylesFields` нічого робити не потрібно. Вони вже повністю реалізовані.<br><br>

5. Метод `EqualsFields(StyleBase other)` повинен порівнювати всі поля поточного та переданого стилю:

```csharp
protected override bool EqualsFields(Base.StyleBase other)
{
    if (other is StyleLayoutContainerUniformGrid style)
    {
        if (RowsCount != style.RowsCount ||
            ColumnsCount != style.ColumnsCount)
            return false;

        if (RowsAlignment != style.RowsAlignment ||
            ColumnsAlignment != style.ColumnsAlignment)
            return false;

        if (!Utils.UtilsStyles.EqualsStyleDimensionFields(RowsSpace, style.RowsSpace) ||
            !Utils.UtilsStyles.EqualsStyleDimensionFields(ColumnsSpace, style.ColumnsSpace))
            return false;

        return true;
    }

    return false;
}
```

---

Наступні методи не визначені в класі `StyleBase`, але їх містить кожен клас стилю даної бібліотеки:

1. Метод `Copy` вже описаний вище.<br><br>
2. Методи `Set`, `SetAllProperties`. Обидва методи приймають усі поля класу. Метод `Set` надає кожному з них параметр за замовчуванням:

```csharp
public void Set(
    int rowsCount = 0,
    int columnsCount = 0,
    Enums.UIExAlignment rowsAlignment = Enums.UIExAlignment.Start,
    Enums.UIExAlignment columnsAlignment = Enums.UIExAlignment.Start,
    StyleDimension rowsSpace = default(StyleDimension),
    StyleDimension columnSpace = default(StyleDimension))
        => SetAllFields(
            rowsCount,
            columnsCount,
            rowsAlignment,
            columnsAlignment,
            rowsSpace,
            columnSpace);

protected void SetAllFields(
    int rowsCount,
    int columnsCount,
    Enums.UIExAlignment rowsAlignment,
    Enums.UIExAlignment columnsAlignment,
    StyleDimension rowsSpace,
    StyleDimension columnSpace)
{
    RowsCount = rowsCount;
    ColumnsCount = columnsCount;

    RowsAlignment = rowsAlignment;
    ColumnsAlignment = columnsAlignment;

    RowsSpace = rowsSpace;
    ColumnsSpace = columnSpace;
}
```

<br>

3. Метод `THIS_CLASS GetCopy()` повертає новий об'єкт класу стилю з усіма поточними полями. Якщо ви реалізували метод `CopyBase` (див. вище), достатньо наступної реалізації:

```csharp
public StyleLayoutContainerUniformGrid GetCopy()
    => GetCopyBase<StyleLayoutContainerUniformGrid>();
```

---

Також усі класи стилю даної бібліотеки мають два конструктори:

1. За замовчуванням (обов'язкова вимога).
2. Той, що приймає всі поля класу (зі значеннями за замовчуванням).

```csharp
public StyleLayoutContainerUniformGrid() { }

public StyleLayoutContainerUniformGrid(
    int rowsCount = 0,
    int columnsCount = 0,
    Enums.UIExAlignment rowsAlignment = Enums.UIExAlignment.Start,
    Enums.UIExAlignment columnsAlignment = Enums.UIExAlignment.Start,
    StyleDimension rowsSpace = default(StyleDimension),
    StyleDimension columnSpace = default(StyleDimension))
        => SetAllFields(
            rowsCount,
            columnsCount,
            rowsAlignment,
            columnsAlignment,
            rowsSpace,
            columnSpace);
```

---

<h2 align="center">Реєстрація стилів компонування</h2>

1. Стилі компонування контейнера повинні бути зареєстровані в класі `StyleLayoutContainer`.
2. Стилі компонування окремого елемента повинні бути зареєстровані в класі `StyleLayoutChild`.<br>

У самій бібліотеці всі класи контейнерів зареєстровані в `StyleLayoutContainer` (аналогічно для `StyleLayoutChild`):

```csharp
public partial class StyleLayoutContainer : Base.StyleLayoutContainerBase
{
    private readonly System.Collections.Generic.Dictionary<System.Type, Base.StyleLayoutContainerBase> _styles = new();

    public StyleLayoutContainerCanvas Canvas() => GetOrCreateStyle<StyleLayoutContainerCanvas>();
    public StyleLayoutContainerStackPanel StackPanel() => GetOrCreateStyle<StyleLayoutContainerStackPanel>();
    public StyleLayoutContainerDockPanel DockPanel() => GetOrCreateStyle<StyleLayoutContainerDockPanel>();
    public StyleLayoutContainerWrapPanel WrapPanel() => GetOrCreateStyle<StyleLayoutContainerWrapPanel>();
    public StyleLayoutContainerGrid Grid() => GetOrCreateStyle<StyleLayoutContainerGrid>();
    public StyleLayoutContainerUniformGrid UniformGrid() => GetOrCreateStyle<StyleLayoutContainerUniformGrid>();

    public T GetOrCreateStyle<T>() where T : Base.StyleLayoutContainerBase, new()
    {
        if (!_styles.TryGetValue(typeof(T), out Base.StyleLayoutContainerBase value))
        {
            value = new T();
            _styles.Add(typeof(T), value);
        }

        return (T)value;
    }
}
```

Для реєстрації вашого стилю потрібно використовувати метод розширення для цього класу. Наприклад:

```csharp
using UIeXtension;
using UIeXtension.Styles;
using UIeXtension.Styles.Base;

namespace Example;

// ваш клас стилю компонування контейнера
public class StyleLayoutContainerExample : StyleLayoutContainerBase 
{ 
    /* 
    
    ... 

    */ 
}


// ваш клас з методом розширення для StyleLayoutContainer
public static class StyleLayoutContainerExtensions
{
    public static StyleLayoutContainerExample Example(this StyleLayoutContainer style)
        => style.GetOrCreateStyle<StyleLayoutContainerExample>();
}


// клас для прикладу отримання вашого стилю
public class ExampleClass
{
    public void Initialize()
    {
        UIExGrid customLayout = new(); // замість UIExGrid - ваш контейнер компонування
        StyleLayoutContainerExample style = customLayout.StyleLayout.Example(); 
    }
}
```

---

<h2 align="center">Про віртуальні методи</h2>

Більша частина методів усіх класів контейнерів — віртуальні.<br>
У рамках даної статті неможливо описати всі методи, які реалізує дана бібліотека.<br>
Усі допоміжні методи класу `UIExLayout` визначені у файлі `UIExLayoutAuxiliaryMethods.cs`.<br>
Якщо ви плануєте створювати власні компоновщики — рекомендується ознайомитися з цим файлом.<br><br>

Усі інші етапи створення компоновщика ви можете взяти з попередньої статті та, вивчивши код компоновщиків даної бібліотеки.

---

Будь ласка, допоможіть цьому проєкту: <a href="https://ko-fi.com/evilcriz">Ko-fi</a>, <a href="https://www.patreon.com/cw/EvilCriz">Patreon</a><br>

* Завдяки вашій допомозі я можу продовжувати займатися цим та іншими проєктами.
* Дякую!<br>

---

<table align="center" width="100%">
    <tr>
        <td align="left"><b>Попередня частина:</b></td>
        <td align="left">
          <a 
            href="2.999.2-LayoutLifeСycle.md">
            Розділ II: Елементи компонування. Частина 10: «Життєвий цикл елементів компонування»
          </a>
        </td>
    </tr>
    <tr>
        <td align="left"><b>Наступна частина:</b></td>
        <td align="left">
          <a 
            href="../Розділ-3/3.1-UIExElement.md">
            Розділ III: Елементи користувацького інтерфейсу. Частина 1: «Клас UIExElement»
          </a>
        </td>
    </tr>
    <tr>
        <td align="left"><b>Зміст:</b></td>
        <td align="left"><a href="../README.md">Перейти до змісту</a></td>
    </tr>
</table>
